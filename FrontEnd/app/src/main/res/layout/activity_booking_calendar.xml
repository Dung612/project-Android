<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent" android:padding="16dp">

    <DatePicker android:id="@+id/datePicker"
        android:layout_width="match_parent" android:layout_height="wrap_content"
        android:calendarViewShown="true"/>

    <LinearLayout android:layout_width="match_parent"
        android:layout_height="wrap_content" android:orientation="horizontal"
        android:gravity="end" android:layout_marginTop="8dp">

        <Button android:text="Xóa" style="@style/Widget.AppCompat.Button.Borderless"/>
        <Button android:text="Hủy" style="@style/Widget.AppCompat.Button.Borderless"/>
        <Button android:text="Chọn"/>
    </LinearLayout>
</LinearLayout>
